# Chains

In LangChain, a Chain is a sequence of steps that connects one or more components (such as prompts, language models, tools, or custom functions) so the output of one step becomes the input to the next. It lets you build multi-step AI workflows instead of making a single LLM call.

## Types

1. Simple chain
2. Sequential chain
3. Parallel chain
4. Conditional chain
